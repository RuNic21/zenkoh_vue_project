---
alwaysApply: true
description: プロジェクト構造とファイル配置ルール（常時適用）
---

# Zenkoh Project Scheduler - プロジェクト構造ルール

## 📁 ディレクトリ構造

### ルートディレクトリ
```
zenkoh_vue_project/
├── src/                    # メインソースコード
├── docs/                   # プロジェクトドキュメント
├── scripts/                # 開発・ビルドスクリプト
├── exports/                # CSVエクスポートファイル
├── material-dashboard-master/ # Material Dashboard テンプレート
├── .cursor/rules/          # Cursor AI ルール
└── package.json            # 依存関係とスクリプト定義
```

### ソースコード構造
```
src/
├── main.js                 # Vue アプリエントリーポイント
├── App.vue                 # ルートコンポーネント
├── layouts/                # レイアウトコンポーネント
│   └── MainLayout.vue      # メインレイアウト
├── pages/                  # ページコンポーネント
│   ├── ScheduleList.vue    # スケジュール一覧
│   └── ScheduleDetail.vue  # スケジュール詳細
├── components/             # 再利用可能コンポーネント
├── services/               # データベース・API サービス
├── store/                  # 状態管理
├── types/                  # TypeScript 型定義
├── utils/                  # ユーティリティ関数
└── assets/                 # 静的リソース
```

## 🏗️ アーキテクチャ原則

### 1. レイヤー分離
- **UI Layer**: Vue コンポーネント（pages/, components/）
- **State Layer**: Store（store/）
- **Service Layer**: データアクセス（services/）
- **Type Layer**: 型定義（types/）
- **Utility Layer**: ヘルパー関数（utils/）

### 2. ファイル命名規則
- **コンポーネント**: PascalCase（例: `ScheduleList.vue`）
- **サービス**: camelCase（例: `taskService.ts`）
- **型定義**: camelCase（例: `schedule.ts`）
- **ユーティリティ**: camelCase（例: `taskAdapter.ts`）

### 3. インポート規則
```typescript
// 相対パス（同じディレクトリ内）
import { helper } from "./helper";

// 絶対パス（src/ からの相対）
import { service } from "@/services/service";

// 外部ライブラリ
import { ref } from "vue";
```

## 📋 主要ファイルの役割

### エントリーポイント
- [src/main.js](mdc:src/main.js): Vue アプリ初期化、Material Dashboard CSS 読み込み
- [src/App.vue](mdc:src/App.vue): ルートコンポーネント、ページルーティング、ダッシュボード

### サービス層（6個のコアファイル）
- [src/services/supabaseClient.ts](mdc:src/services/supabaseClient.ts): Supabase 接続クライアント
- [src/services/crud.ts](mdc:src/services/crud.ts): 汎用 CRUD ファクトリ
- [src/services/dbServices.ts](mdc:src/services/dbServices.ts): 自動生成 CRUD リポジトリ
- [src/services/taskService.ts](mdc:src/services/taskService.ts): タスク専用サービス
- [src/services/projectService.ts](mdc:src/services/projectService.ts): プロジェクト専用サービス
- [src/services/relationService.ts](mdc:src/services/relationService.ts): 関係型データサービス

### 状態管理
- [src/store/schedule.ts](mdc:src/store/schedule.ts): スケジュール状態管理、DB 連携

### 型定義
- [src/types/db/](mdc:src/types/db/): Supabase スキーマ自動生成型（編集禁止）
- [src/types/schedule.ts](mdc:src/types/schedule.ts): UI 表示用型
- [src/types/task.ts](mdc:src/types/task.ts): ビジネスロジック用型
- [src/types/project.ts](mdc:src/types/project.ts): プロジェクト用型

### ユーティリティ
- [src/utils/taskAdapter.ts](mdc:src/utils/taskAdapter.ts): DB ↔ UI 型変換
- [src/utils/uiHelpers.ts](mdc:src/utils/uiHelpers.ts): UI ヘルパー関数
- [src/utils/dateUtils.ts](mdc:src/utils/dateUtils.ts): 日付処理

## 🔧 開発スクリプト

### 基本コマンド
```bash
npm run dev          # 開発サーバー起動
npm run build        # プロダクションビルド
npm run preview      # ビルド結果プレビュー
```

### データベース管理
```bash
npm run test:env              # 環境変数・DB接続テスト
npm run test:crud:all         # 全テーブルCRUDテスト
npm run seed:all              # シードデータ生成
npm run export:csv            # CSVエクスポート
npm run debug:count           # レコード数確認
```

### 型生成
```bash
npm run types:gen             # Supabaseから型自動生成
npm run types:fromcsv         # CSVから型生成
```

## 📦 依存関係

### 主要ライブラリ
- **Vue 3**: フロントエンドフレームワーク
- **Vite**: ビルドツール
- **Supabase**: データベース・認証
- **Material Dashboard 3**: UI フレームワーク
- **Bootstrap 5**: CSS フレームワーク

### 開発ツール
- **TypeScript**: 型安全性
- **Sass**: CSS プリプロセッサ
- **Chart.js**: グラフ表示
- **Font Awesome**: アイコン

## 🚨 重要な制約

### 1. ファイル編集制限
- `src/types/db/*.ts`: 自動生成ファイル（編集禁止）
- `material-dashboard-master/`: テンプレートファイル（参照のみ）

### 2. 環境変数必須
- `.env.local` に Supabase 接続情報設定必須
- 環境変数なしでは DB 接続不可

### 3. サービス層使用
- コンポーネントから直接 DB アクセス禁止
- Store または Service レイヤー経由でアクセス

## 📚 関連ドキュメント

- [データベーススキーマ](../docs/database-schema.md)
- [データベース統合ガイド](../docs/database-integration-guide.md)
- [README](../README.md)