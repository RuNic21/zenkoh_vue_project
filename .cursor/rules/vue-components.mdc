---
alwaysApply: true
description: Vue コンポーネント開発ルール（日本語）
---

# Vue コンポーネント開発ルール（日本語）

## コンポーネント構造（推奨）
```vue
<script setup>
// このコンポーネントの目的を簡潔に説明
import { ref, computed, onMounted } from "vue";

const props = defineProps({
  // 受け取るデータの用途を説明
  propName: { type: String, required: false, default: "" },
});

const emit = defineEmits(["navigate", "update", "delete"]);

const state = ref("");
const derived = computed(() => state.value);

const handleEvent = () => {
  // イベント処理の意図を説明
  emit("update", state.value);
};

onMounted(() => {
  console.log("マウント完了");
});
</script>

<template>
  <!-- ルート要素の役割を説明 -->
  <div class="component-name">
    <!-- セクション説明 -->
    <header></header>
    <main></main>
  </div>
</template>

<style scoped>
/* 必要な範囲のみスタイルを適用 */
</style>

## 日本語テキスト/エラー
- 画面の文言は日本語で統一
- try/catch で失敗時の日本語メッセージを提示
- ネットワーク/バリデーション等のエラー種別を区別
